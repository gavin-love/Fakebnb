<JumboHeader>
  <h2>Welcome to Fakebnb!</h2>
  <p>Fakebnb is the place to be! Don't miss out! Book Now.</p>
  <LinkTo @route="about" class="button about">About Us</LinkTo>
  <LinkTo @route="contact" class="button contact">Contact Us</LinkTo>
</JumboHeader>

<div class="rentals">
  <ul class="results">
    {{log 'model' @model}}
    {{#each @model as |rental|}}
      <li><RentalCard @rental={{rental}} /></li>
    {{/each}}
  </ul>
</div>